name: Sayan's Daily Activity Booster

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  update-text:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Update Activity Log
        run: |
          echo "Activity Log - Sayan" > last_activity.txt
          echo "Last Update: $(date)" >> last_activity.txt
          echo "Session ID: $RANDOM" >> last_activity.txt

      - name: Commit & Push as Sayan
        run: |
          git config --global user.name "kajju027"
          git config --global user.email "kajju3864@gmail.com"
          
          git add last_activity.txt
          git commit -m "Update Activity log 🟢 [$(date)]" --allow-empty
          git push

